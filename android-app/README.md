# Android App (post-MVP stub)

This module is not included in the default Gradle build.

To enable it, add `include(":android-app")` to `settings.gradle.kts` and restore Android plugins in the root `build.gradle.kts`. Requires JDK 17+ and Android SDK.
